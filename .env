# Local Development Database Configuration
# These credentials match the oc-infra local setup and are safe to commit
# Production deployments should override DATABASE_URL via cluster secrets
LOCAL_DB_NAME="db"
LOCAL_DB_USER="app"
LOCAL_DB_PASS="StrongLocalPass"
DATABASE_URL="postgresql://${LOCAL_DB_USER}:${LOCAL_DB_PASS}@localhost:5432/${LOCAL_DB_NAME}?schema=public"